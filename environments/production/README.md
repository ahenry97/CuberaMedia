# Production Environment

Use this directory for production setup notes and non-secret configuration examples.

Production changes must be approved through:

- a pull request into `main`;
- the GitHub `production` environment approval gate;
- the production migration workflow.

Never store production secrets in this directory. Store production secrets in GitHub Actions secrets or the hosting provider secret manager.
