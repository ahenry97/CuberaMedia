name: Local testing backup

on:
  push:
    branches:
      - develop
      - "feature/**"
      - "codex/**"
  pull_request:
    branches:
      - develop

permissions:
  contents: read

jobs:
  validate-local-testing:
    name: Validate local testing structure
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate expected project directories
        run: |
          test -d src/app
          test -d environments/local
          test -d migrations

      - name: Upload app source snapshot
        uses: actions/upload-artifact@v4
        with:
          name: app-source-snapshot-${{ github.sha }}
          path: |
            src
            package.json
            supabase
          retention-days: 30
